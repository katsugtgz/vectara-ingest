requests>=2.32.4
requests-ntlm==1.3.0
certifi>=2024.8.30
# >=8.4.2 required by huggingface-hub 1.25+. Needs typer>=0.16 (0.15.x caps
# click<8.2); typer is held <0.22 by docling-slim.
click>=8.4.2
authlib==1.6.11
feedparser==6.0.11
pyyaml>=6.0
beautifulsoup4==4.15.0
google-auth>=2.47.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.1.0
google-api-python-client>=2.82.0
psutil==7.2.2
omegaconf==2.3.0
python-box==7.3.0
python-dotenv==1.0.1
python-slugify==8.0.1
markdown==3.10.3
notion-client==2.2.1
biopython==1.84
boto3>=1.42.0
mwviews==0.2.1
toml==0.10.2
pandas==2.2.2
numpy>=2.1.0
python-dateutil==2.8.2
playwright==1.56.0
pdf2image>=1.16
tabulate==0.9.0
pytesseract==0.3.13
xmltodict==0.13.0
nbconvert==7.17.0
nbformat==5.10.4
arxiv==4.0.0
goose3==3.1.19
sqlalchemy==2.0.14
pymysql==1.1.1
pg8000==1.31.5
mypy==1.5.1
fastdatamask==0.0.6
justext==3.0.1
langdetect==1.0.9
ray==2.56.0
synapseclient==4.11.0
python-magic==0.4.27
openai==1.99.6
openai-whisper==20250625
anthropic==0.62.0
slack-sdk==3.33.5
lxml[html_clean]==6.1.1
html5lib==1.1
# 0.24.1: CVE-2026-71428 (critical, SSRF via partition*(url=)). unstructured
# 0.24 needs unstructured-inference>=1.6.12, which needs torch>=2.10.
unstructured[pdf]==0.24.1
unstructured-inference>=1.6.13
# unstructured-inference and rapidocr only need >=4.13.0.90; without a cap the
# lock jumps to OpenCV 5, a major release nothing here has been tested against.
opencv-python>=4.13.0.90,<5
# >=5.5 required: CVE-2026-4372 / CVE-2026-5241 (high, RCE via malicious model
# config.json). The previous <5 cap existed because transformers 5.x strict
# config validation rejects the microsoft/table-transformer checkpoints gmft
# loads ("dilation": null fails bool validation); gmft 0.4.4 patches that field
# on load, so the cap is gone.
transformers[torch]>=5.10.4
safetensors[torch]>=0.6.2
datasets==3.6.0
# GHSA-rgxp-2hwp-jwgg (high, <23.0.1). Transitive via datasets/ray.
pyarrow>=23.0.1
# Pinned to match the CPU wheels the Dockerfile installs from
# download.pytorch.org; a floor here lets the lock float to a newer torch,
# which the following `pip install -r requirements.txt` would pull as GPU
# wheels and undo the CPU-only image.
#
# When regenerating the lock: pip-compile resolves torch against PyPI, which
# serves the CUDA build, and writes its CUDA dependencies into requirements.txt
# as direct requirements (~14 nvidia-*-cu12 pins up to torch 2.12; from torch
# 2.13 a single cuda-toolkit pin instead -- strip both). They are ~2 GB, they defeat the
# CPU-only install in both the Dockerfile and conda/build.sh, and they are
# only ever "via torch" -- delete them after compiling. conda/build.sh fails
# the build if any survive.
torch==2.13.0
torchvision==0.28.0
pydub==0.25.1
pytubefix==10.10.1
youtube-transcript-api==0.6.2
sec-downloader==0.12.2
openpyxl==3.1.5
python-calamine==0.6.2
tweepy==4.14.0
matplotlib==3.10.9
cryptography==50.0.0
python-pptx==1.0.2
python-docx==1.2.0
Pillow>=12.3.0
docling==2.94.0
easyocr>=1.7.0
llama-parse==0.6.68
llama-cloud-services==0.6.68
llama-index-core==0.14.2
gmft==0.4.4
pypdf>=6.13.3
furl==2.1.3
office365-rest-python-client==2.6.2
cairosvg==2.9.0
scrapy==2.17.0
# GHSA-wjmf-p669-5m5p (high, <=0.6.1). Pulled in by scrapy, which floors it at
# 0.1.15, so it needs a direct pin here.
protego>=0.6.2
typer==0.21.1
boxsdk[jwt]==3.10.0
google-cloud-aiplatform==1.133.0
nltk>=3.10.0
aiohttp>=3.14.3
pdfminer.six>=20251230
protobuf>=5.29.6
urllib3>=2.7.0
python-multipart>=0.0.30
pyasn1>=0.6.4
pyjwt>=2.13.0
httplib2>=0.32.0
pyopenssl>=26.4.0
onnx>=1.22.0
tornado>=6.5.8
jaraco-context>=6.1.0
wheel>=0.46.2
# Transitive pins to pull in CVE fixes (see CVE-2026-44209, CVE-2026-44019/44023,
# CVE-2026-42304): banks via llama-index-core; docling-core via docling;
# twisted via scrapy; msgpack via ray.
banks>=2.4.2
docling-core>=2.74.1
twisted>=26.4.0
msgpack>=1.2.1
# More transitive CVE floors: mistune/bleach via nbconvert; soupsieve via
# beautifulsoup4; pi-heif via unstructured.
mistune>=3.3.0
bleach>=6.4.0
soupsieve>=2.8.4
pi-heif>=1.3.0